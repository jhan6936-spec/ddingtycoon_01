# Copy to .env locally if you use a tool that loads it before `npm run build`,
# or set these in the Vercel project → Settings → Environment Variables.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key
